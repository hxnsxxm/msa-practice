server:
  port: 8000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka # eureka server

spring:
  application:
    name: apigateway-service
  cloud:
    gateway:
      default-filters:
        - name: GlobalFilter
          args:
            baseMessage: Spring Cloud Gateway Global Filter
            preLogger: true
            postLogger: true
      routes:
        - id: user-service
          uri: lb://USER-SERVICE # spring.application.name
          predicates: # ???
            - Path=/user-service/** # ???? ?? path
        - id: catalog-service
          uri: lb://CATALOG-SERVICE # spring.application.name
          predicates: # ???
            - Path=/catalog-service/** # ???? ?? path
        - id: order-service
          uri: lb://ORDER-SERVICE # spring.application.name
          predicates: # ???
            - Path=/order-service/** # ???? ?? path
        - id: first-service
          uri: lb://MY-FIRST-SERVICE # spring.application.name
          predicates: # ???
            - Path=/first-service/** # ???? ?? path
          filters:
#            - AddRequestHeader=first-request, first-request-header2
#            - AddResponseHeader=first-response, first-response-header2
            - CustomFilter
        - id: second-service
          uri: lb://MY-SECOND-SERVICE
          predicates:
            - Path=/second-service/**
          filters:
#            - AddRequestHeader=second-request, second-request-header2
#            - AddResponseHeader=second-response, second-response-header2
            - name: CustomFilter
            - name: LoggingFilter # args ?? ??? name? ???? ?(first service? ?????)
              args:
                baseMessage: Hi, there.
                preLogger: true
                postLogger: true