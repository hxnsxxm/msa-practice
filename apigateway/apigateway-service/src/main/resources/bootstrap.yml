spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: config-service
#  profiles:
#    active: dev  # Deprecated: spring.profiles.active -> spring.config.activate.on-profile ?

#  config:
#    activate:
#      on-profile: prod
