server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/Projects/config-native
        git:
          uri: https://github.com/hxnsxxm/spring-cloud-config # .git
          #uri: file:///Users/jeonghansaem/Projects/msa-local
#          username: # private 시 설정
#          password: # private 시 설정
